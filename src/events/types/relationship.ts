export enum RelationshipType {
  Active = "active",
  Blocked = "blocked"
}

export enum RelationshipUpdateType {
  Requested = "requested",
  Accepted = "accepted",
  Cancelled = "cancelled",
  Rejected = "rejected",
  Blocked = "blocked",
  Unblocked = "unblocked"
}
